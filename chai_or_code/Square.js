function addistion(a, b) {
  const add = a + b;
  return add;
}

function square(x) {
  const sq = x * x;
  return sq;
}
function printResult(r) {
  console.log(r);
}
const res = addistion(50, 50);
let p = square(res);
printResult(p);
